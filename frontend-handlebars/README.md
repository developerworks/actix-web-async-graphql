```shell

Adding async-std v1.11.0 to dependencies.
     Features:
     + alloc
     + async-channel
     + async-global-executor
     + async-io
     + async-lock
     + crossbeam-utils
     + futures-channel
     + futures-core/alloc
     + futures-core/std
     + futures-io
     + futures-lite
     + gloo-timers
     + kv-log-macro
     + log
     + memchr
     + num_cpus
     + once_cell
     + pin-project-lite
     + pin-utils
     + slab
     + std
     + wasm-bindgen-futures
     - async-attributes
     - async-process
     - attributes
     - docs
     - futures-core
     - surf
     - tokio02
     - tokio03
     - tokio1
     - unstable
Adding tide v0.16.0 to dependencies.
     Features:
     + async-h1
     + async-session
     + cookies
     + femme
     + h1-server
     + http-types/cookies
     + logger
     + sessions
     - docs
     - unstable
Adding serde v1.0.136 to dependencies.
     Features:
     + std
     - alloc
     - derive
     - rc
     - serde_derive
     - unstable
Adding serde_json v1.0.79 to dependencies.
     Features:
     + serde/std
     + std
     - alloc
     - arbitrary_precision
     - float_roundtrip
     - indexmap
     - preserve_order
     - raw_value
     - unbounded_depth
Adding handlebars v4.2.2 to dependencies.
     Features:
     - dir_source
     - no_logging
     - rhai
     - rust-embed
     - script_helper
     - walkdir
```